# Normalize line endings
* text=auto eol=lf

# Windows scripts keep CRLF
*.bat text eol=crlf

# Unix shell / gradle wrapper must stay LF (executed on Linux CI runners)
gradlew text eol=lf

# Treat binaries as binary
*.jar binary
*.png binary
*.keystore binary
*.apk binary
